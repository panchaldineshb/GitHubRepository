// Values in the array are converted to the appropriate data type
// and then stored in the view. The constructor also creates a new 
// ArrayBuffer to hold the values. 

var view = new Uint16Array([1, 2, 3]);